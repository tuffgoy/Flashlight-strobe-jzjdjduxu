{
  "name": "flashlight-strobe-workspace",
  "version": "1.5.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "typecheck": "pnpm --filter @workspace/flashlight-strobe run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
